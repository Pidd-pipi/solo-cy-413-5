<!-- 改编自：cy-411-420混合主题项目提示词/cy-413/cy-413.md；后端由 FastAPI+Python 3.11 改造为 Go 1.22 + Gin + GORM -->
# cy-413 心理健康与情绪日记

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**心理健康与情绪日记（MindGarden）** 是一款帮助用户记录情绪变化、进行心理测评、收听冥想音频的个人心理健康应用。系统通过情绪曲线、标签统计、测评报告帮助用户了解自身心理状态。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, email, password_hash, nickname, avatar, birth_date, gender, created_at
  - 关键关系：拥有多条 Mood、Assessment、Journal 记录
  - 业务动作：注册/登录、修改资料、查看心理报告
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts**

- **Mood（情绪记录）**
  - 字段：id, user_id, mood_level（1-10）, mood_tags（焦虑/开心/疲惫/愤怒/平静）, note, record_date, created_at
  - 关键关系：隶属于 User
  - 业务动作：快速记录情绪、按日期筛选、趋势分析
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **Assessment（心理测评）**
  - 字段：id, title, description, category（焦虑/抑郁/压力/睡眠）, questions（JSON）, scoring_rule（JSON）
  - 关键关系：用户通过 UserAssessment 参与测评
  - 业务动作：选择测评、答题、生成结果与建议
  - **此实体必须贯穿全栈**

- **Journal（日记）**
  - 字段：id, user_id, title, content, mood_level, weather, is_private, created_at
  - 关键关系：隶属于 User，与 Mood 可联动
  - 业务动作：写日记、编辑、删除、查看历史
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/dashboard（心情花园）**
  - 消费实体：User、Mood、Assessment
  - 关键交互：本周情绪曲线、最新测评推荐、快速记录情绪
  - 共享组件：`<MoodTrendChart>`、`<MoodSelector>`

- **/moods（情绪记录）**
  - 消费实体：Mood
  - 关键交互：情绪日历、列表筛选、新增/编辑情绪
  - 共享组件：`<MoodSelector>`（与 /dashboard 共用）、`<MoodCard>`

- **/assessments（心理测评）**
  - 消费实体：Assessment、User
  - 关键交互：测评列表、答题页面、结果报告
  - 共享组件：`<AssessmentCard>`、`<ResultBadge>`

- **/journals（日记本）**
  - 消费实体：Journal、Mood
  - 关键交互：富文本日记、按心情筛选、时间轴展示
  - 共享组件：`<MoodCard>`（与 /moods 共用）、`<EmptyState>`

- **/profile（个人中心）**
  - 消费实体：User
  - 关键交互：修改资料、查看心理报告汇总
  - 共享组件：`<AvatarUploader>`

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + 角色）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段
    - 后端：`internal/middleware/auth.go`、`internal/util/jwt.go`、`internal/constants/roles.go`
    - 路由：写操作需认证，管理员可操作 Assessment
    - 前端：`router/guards.ts`、`stores/authStore.ts`

- **主题切换**
  - 触达文件层：
    - 前端：`stores/themeStore.ts`、`constants/themes.ts`
    - 组件：多个组件读取 theme 状态并切换 class/style
    - 工具：`utils/themeUtils.ts`
    - 样式：多套 CSS 变量定义

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`
    - 前端：`utils/request.ts` 拦截器、`components/common/GlobalErrorBoundary.tsx`

### 5. 共享枚举/常量（≥ 2 组）

- **MoodTag（情绪标签）**
  - 值：HAPPY = 'happy', ANXIOUS = 'anxious', TIRED = 'tired', ANGRY = 'angry', CALM = 'calm'
  - 前端定义位置：`frontend/src/constants/mood.ts`
  - 后端定义位置：`backend/internal/constants/mood.go`
  - 出现位置：Mood 模型、前后端类型定义、MoodSelector、MoodCard、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **AssessmentCategory（测评分类）**
  - 值：ANXIETY = 'anxiety', DEPRESSION = 'depression', STRESS = 'stress', SLEEP = 'sleep'
  - 前端定义位置：`frontend/src/constants/assessment.ts`
  - 后端定义位置：`backend/internal/constants/assessment.go`
  - 出现位置：Assessment 模型、测评列表页、筛选器、日志模板
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<MoodTrendChart>`：被 /dashboard 使用
- `<MoodSelector>`：被 /dashboard 和 /moods 共用
- `<MoodCard>`：被 /moods 和 /journals 共用
- `<AssessmentCard>`：被 /assessments 使用
- `<ResultBadge>`：被 /assessments 使用
- `<EmptyState>`：被多个列表页共用
- `<AvatarUploader>`：被 /profile 使用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useAuth()`：登录态管理
- `useTheme()`：主题切换
- `useMoodStats()`：情绪统计汇总
- `utils/moodColor.ts`：情绪等级/标签对应颜色
- `utils/dateRange.ts`：生成日期范围

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：JWT 验证
- `error_handler.go`：统一错误处理
- `audit_logger.go`：记录用户写操作

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`mindgarden`
- **前端端口**：18413
- **后端端口**：19413
- **前端框架**：React 18 + TypeScript
- **UI 库**：Ant Design + Recharts
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL 15
- **ORM**：GORM
- **认证方式**：JWT（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, mood.ts, assessment.ts, journal.ts
├── stores/           # authStore.ts, userStore.ts, moodStore.ts, themeStore.ts
├── types/            # 共享类型定义
├── components/common/# MoodTrendChart, MoodSelector, MoodCard, AssessmentCard, ResultBadge, EmptyState, AvatarUploader
├── hooks/            # useAuth.ts, useTheme.ts, useMoodStats.ts
├── pages/            # Dashboard, Moods, Assessments, Journals, Profile
├── router/           # index.tsx + guards.ts
├── utils/            # moodColor.ts, dateRange.ts, request.ts
└── constants/        # mood.ts, assessment.ts, themes.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go
    ├── model/                   # user.go, mood.go, assessment.go, journal.go, user_assessment.go
    ├── repository/              # user_repository.go, mood_repository.go, assessment_repository.go, journal_repository.go
    ├── service/                 # user_service.go, mood_service.go, assessment_service.go, journal_service.go
    ├── handler/                 # user_handler.go, mood_handler.go, assessment_handler.go, journal_handler.go
    ├── router/                  # router.go + users.go, moods.go, assessments.go, journals.go
    ├── middleware/              # auth.go, error_handler.go, audit_logger.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # mood.go, assessment.go, roles.go, themes.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, password.go, logger.go, mood_color.go, formatters.go, app_error.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，所有 handler/service/middleware 都引用
   - 日志模板集中在 `internal/constants/log_templates.go`，包含至少 20 条模板
   - 任何业务字段变更（如 Mood 新增字段）都必须同步修改日志模板

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但 service 和 handler 都要手动包装
   - 错误 message 必须包含实体名、字段名，如 `"Mood[id=123] update failed: mood_level out of range"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 同时包含日期格式化、情绪文本、测评分类文本、主题色转换
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **主题切换必须牵一发而动全身**
   - 主题色在 `internal/constants/themes.go`、前端 CSS 变量、ECharts 配置、Ant Design ConfigProvider、`internal/util/formatters.go` 中同时存在
   - 新增主题需要修改至少 8 处

5. **枚举必须前后端重复定义且被多处引用**
   - MoodTag 和 AssessmentCategory 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters 中同时存在
   - 新增一个枚举值时，必须修改至少 8 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 Mood 新增一个 'excited' 标签"，必须触达：数据库 schema/GORM AutoMigrate、Mood 模型、前后端 constants、类型定义、MoodSelector、MoodCard、moodColor utils、日志模板、错误码、formatters、Dashboard 图表等 ≥ 10 个文件
## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=mindgarden`
- `frontend/Dockerfile`
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`
- `.env.example`

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: mindgarden`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-mindgarden}` 前缀
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`）
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`
- 暴露端口：前端 `18413:80`，后端 `${BACKEND_PORT:-19413}:8080`，数据库 `5432:5432`
- 所有可变配置通过环境变量注入
- 支持通过 `docker compose up -d` 一条命令启动

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=mindgarden`
- `DB_NAME=mindgarden_db`、`DB_USER=mindgarden_user`、`DB_PASSWORD=mindgarden_pwd`
- `DB_ROOT_PASSWORD` 改为 `DB_ADMIN_PASSWORD`
- `JWT_SECRET=change_me_to_a_long_random_string`
- `FRONTEND_PORT=18413`、`BACKEND_PORT=19413`、`DB_PORT=5432`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
frontend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 MoodTag 和 AssessmentCategory 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:18413` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<MoodSelector>` 或 `<MoodCard>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
