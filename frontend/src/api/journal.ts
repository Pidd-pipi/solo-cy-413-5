import {request} from '../utils/request';import type {Journal} from '../types';export const listJournals=(level?:number)=>request<Journal[]>(`/journals${level?`?mood_level=${level}`:''}`);export const saveJournal=(payload:Omit<Journal,'id'|'created_at'|'updated_at'>)=>request<Journal>('/journals',{method:'POST',body:JSON.stringify(payload)});export const deleteJournal=(id:number)=>request(`/journals/${id}`,{method:'DELETE'});
