import {request} from '../utils/request'; import type {User,UserAssessment} from '../types'; export const login=(email:string,password:string)=>request<{token:string;user:User}>('/auth/login',{method:'POST',body:JSON.stringify({email,password})}); export const register=(email:string,password:string,nickname:string)=>request<{token:string;user:User}>('/auth/register',{method:'POST',body:JSON.stringify({email,password,nickname})}); export const getMe=()=>request<User>('/users/me'); export const updateMe=(payload:Partial<User>)=>request<User>('/users/me',{method:'PUT',body:JSON.stringify(payload)}); export const getReports=()=>request<UserAssessment[]>('/users/reports');
