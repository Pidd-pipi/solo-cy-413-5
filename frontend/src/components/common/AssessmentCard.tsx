import {Button,Card,Tag} from 'antd';import {ASSESSMENT_LABELS} from '../../constants/assessment';import type {Assessment} from '../../types';export function AssessmentCard({assessment,onTake}:{assessment:Assessment;onTake:()=>void}){return <Card title={assessment.title} extra={<Tag color="purple">{ASSESSMENT_LABELS[assessment.category]}</Tag>}><p>{assessment.description}</p><Button type="primary" onClick={onTake}>开始测评</Button></Card>}
