import {Avatar,Button,Input,Space} from 'antd';import {useState} from 'react';export function AvatarUploader({value,onChange}:{value?:string;onChange:(v:string)=>void}){const [url,setUrl]=useState(value||'');return <Space direction="vertical"><Avatar size={64} src={url}>{!url&&'MG'}</Avatar><Input value={url} placeholder="粘贴头像图片地址" onChange={e=>setUrl(e.target.value)}/><Button onClick={()=>onChange(url)}>保存头像</Button></Space>}
