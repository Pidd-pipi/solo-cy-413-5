import {Card,Tag,Typography} from 'antd';import {MOOD_LABELS} from '../../constants/mood';import {moodColor,tagColor} from '../../utils/moodColor';import type {Mood,MoodTag} from '../../types';export function MoodCard({mood}:{mood:Mood}){let tags:MoodTag[]=[];try{tags=JSON.parse(mood.mood_tags)}catch{}return <Card size="small" className="mood-card"><div className="mood-card__top"><b style={{color:moodColor(mood.mood_level)}}>心情 {mood.mood_level}/10</b><span>{mood.record_date.slice(0,10)}</span></div><div>{tags.map(tag=><Tag color={tagColor(tag)} key={tag}>{MOOD_LABELS[tag]}</Tag>)}</div>{mood.note&&<Typography.Paragraph className="muted">{mood.note}</Typography.Paragraph>}</Card>}
