import {Button,Space,Tooltip} from 'antd';import {MOOD_EMOJI,MOOD_LABELS,MoodTagValues} from '../../constants/mood';import type {MoodTag} from '../../types';export function MoodSelector({value,onChange}:{value:MoodTag[];onChange:(v:MoodTag[])=>void}){return <Space wrap>{Object.values(MoodTagValues).map(tag=><Tooltip title={MOOD_LABELS[tag]} key={tag}><Button aria-label={`选择${MOOD_LABELS[tag]}`} type={value.includes(tag)?'primary':'default'} onClick={()=>onChange(value.includes(tag)?value.filter(v=>v!==tag):[...value,tag])}>{MOOD_EMOJI[tag]} {MOOD_LABELS[tag]}</Button></Tooltip>)}</Space>}
