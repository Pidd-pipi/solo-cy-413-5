import {Empty} from 'antd';import {Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import type {Mood} from '../../types';export function MoodTrendChart({moods}:{moods:Mood[]}){const data=[...moods].reverse().map(m=>({date:m.record_date.slice(5,10),level:m.mood_level}));if(!data.length)return <Empty description="记录一次心情，曲线会在这里生长"/>;return <div style={{width:'100%',height:240}}><ResponsiveContainer><LineChart data={data}><XAxis dataKey="date"/><YAxis domain={[1,10]}/><Tooltip/><Line type="monotone" dataKey="level" stroke="var(--primary)" strokeWidth={3}/></LineChart></ResponsiveContainer></div>}
